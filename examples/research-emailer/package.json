{
  "name": "@glassbox/example-research-emailer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Structured demo agent: topic -> search -> read -> draft -> send email (side-effecting) -> confirm.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@glassbox/engine": "workspace:*",
    "zod": "^4.4.3"
  }
}
